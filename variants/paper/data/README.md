# data/

Everything in this folder except this README is **git-ignored**.

- Document every dataset in the **Data provenance** table in the top-level README.
- Restricted / licensed / personal data must never be committed or pushed anywhere on GitHub.
- Point scripts at data via config or environment variables (e.g. `DATA_DIR`),
  not hard-coded absolute paths.

## Obtaining the data

<!-- TODO: step-by-step instructions (download commands, who to ask for access). -->
